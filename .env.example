# Server
PORT=3000

# PostgreSQL database connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=stocktalk

# Set to true only in development. It auto-creates tables from entities.
DB_SYNCHRONIZE=true
